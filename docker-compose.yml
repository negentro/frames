services:
  agent:
    build:
      context: .
      dockerfile: packages/agent/Dockerfile
    ports:
      - "8787:8787"
    env_file:
      - packages/agent/.env
    environment:
      - PORT=8787
      - PROJECTS_DIR=/tmp/eigen-projects
